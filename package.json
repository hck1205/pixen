{
  "name": "pixen",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -b packages/core packages/web packages/video packages/react packages/vue packages/svelte",
    "typecheck": "tsc -b packages/core packages/web packages/video packages/react packages/vue packages/svelte && tsc --noEmit -p tsconfig.tests.json && pnpm --filter @pixen/stories exec tsc --noEmit -p tsconfig.json && pnpm --filter @pixen/playground exec tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:browser": "pnpm build && pnpm playground:build && playwright test",
    "test:visual": "pnpm build && pnpm stories:build && PIXEN_VISUAL=1 playwright test visual.spec.ts",
    "test:visual:update": "pnpm build && pnpm stories:build && PIXEN_VISUAL=1 playwright test visual.spec.ts --update-snapshots",
    "playground": "pnpm --filter @pixen/playground dev",
    "playground:build": "pnpm --filter @pixen/playground build",
    "clean": "rm -rf packages/*/dist packages/*/tsconfig.tsbuildinfo apps/*/dist apps/stories/build test-results playwright-report",
    "check:independence": "node scripts/independence-scan.mjs",
    "stories": "pnpm --filter @pixen/stories dev",
    "stories:build": "node scripts/build-stories.mjs",
    "check:exports": "node scripts/unused-exports.mjs",
    "check:duplication": "node scripts/duplicate-blocks.mjs",
    "check:size": "node scripts/module-budget.mjs",
    "check:surface": "node scripts/public-surface.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "browserslist": [
    "chrome >= 84",
    "edge >= 84",
    "firefox >= 90",
    "safari >= 15",
    "ios_saf >= 15",
    "not dead"
  ]
}
